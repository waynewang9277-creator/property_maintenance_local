# 物业维保管理系统 APP 问题总结

## 一、项目基本信息

### 1.1 项目名称
物业维保管理系统

### 1.2 项目类型
Android 原生 APP（基于 WebView 混合开发模式）

### 1.3 开发方式
- **核心框架**：纯 HTML5 + CSS + JavaScript（不使用 uni-app 或其他跨平台框架）
- **打包工具**：Android Studio（手动配置原生 Android 项目）
- **APK 包名**：`com.example.propertymaintenance`
- **应用名称**：物业维保管理系统

### 1.4 项目结构
```
PropertyMaintenance/
├── app/src/main/
│   ├── assets/           # Web 资源目录（HTML/CSS/JS/图片等）
│   │   ├── index.html    # 主入口页面
│   │   ├── common/       # 公共资源
│   │   │   ├── css/      # 样式文件
│   │   │   ├── js/       # JavaScript 库文件
│   │   │   ├── images/   # 图片资源
│   │   │   └── models/   # OCR 语言模型
│   │   ├── modules/      # 功能模块
│   │   │   ├── battery-test/         # 应急电池放电测试
│   │   │   ├── busbar-check/         # 供电母排检查及测温
│   │   │   ├── capacitor-check/       # 电容检查及测温
│   │   │   ├── elevator-escape/      # 电梯逃生门检查
│   │   │   ├── elevator-handrail-fault/ # 电梯扶手梯故障报告
│   │   │   ├── generator/            # 发电机组维护性运转记录
│   │   │   └── power-room-monthly/   # 强电间月度巡检
│   │   └── assets/templates/         # Excel 模板文件
│   ├── java/             # Java 原生代码
│   │   └── com/example/propertymaintenance/
│   │       └── MainActivity.java     # 主Activity，承载 WebView
│   └── res/              # Android 资源文件（图标、布局等）
```

### 1.5 关键技术栈

#### 前端技术
- **UI 框架**：原生 HTML5 + CSS（自定义样式，无框架依赖）
- **JavaScript 库**：
  - jsPDF 1.4.1（PDF 生成）
  - html2canvas（页面截图）
  - JSZip（Excel 文件处理）
  - xlsx.full.min.js（Excel 解析与生成）
  - Tesseract.js 4.1.4（OCR 文字识别，用于热成像仪温度读取）

#### Android 原生层
- **WebView**：使用 Android 系统 WebView 加载本地 HTML
- **Java 版本**：Java 8
- **最低 SDK**：21（Android 5.0）
- **目标 SDK**：34（Android 14）

#### 开发工具
- **Android Studio**：5.07 版本
- **JDK**：随 Android Studio 捆绑
- **Gradle**：8.0

---

## 二、功能模块说明

### 2.1 模块列表

| 模块名称 | 文件路径 | 主要功能 |
|---------|---------|---------|
| 应急电池放电测试 | modules/battery-test/ | 电池放电测试记录，生成 PDF 报告 |
| 供电母排检查及测温 | modules/busbar-check/ | 母排温度检测，OCR 识别热成像照片 |
| 电容检查及测温 | modules/capacitor-check/ | 36 个位置电容检查，生成 Excel 报告 |
| 电梯逃生门检查 | modules/elevator-escape/ | 巡检记录（占位模块，功能简单） |
| 电梯扶手梯故障报告 | modules/elevator-handrail-fault/ | 电梯故障记录，生成 Excel 统计报表 |
| 发电机组维护性运转记录 | modules/generator/ | 发电机组维护，生成 Excel 报告 |
| 强电间月度巡检 | modules/power-room-monthly/ | 月度巡检记录，生成 PDF 报告 |

### 2.2 核心功能
- **PDF 生成**：使用 jsPDF 库，支持中文字体，生成检测报告
- **Excel 生成**：使用 JSZip + xlsx 库，处理 Excel 模板，生成数据报表
- **OCR 识别**：使用 Tesseract.js 识别热成像仪照片中的温度数据（Max/Min/Avg）
- **本地存储**：使用 localStorage 保存历史记录
- **拍照功能**：调用手机相机拍摄现场照片

---

## 三、当前存在的问题

### 3.1 返回按钮功能异常

#### 问题描述
进入模块页面后，点击返回按钮没有任何反应。按键事件未被正确处理。

#### 技术分析
1. **根本原因**：WebView 中加载的 iframe 页面使用 `parent.app.backToCategory()` 或 `parent.location` 等方式尝试访问父页面对象，但由于 **CORS（跨域资源共享）策略限制**，这些访问被浏览器安全策略阻止。

2. **错误日志示例**：
```
Uncaught SecurityError: Failed to read a named property 'app' from 'Window': 
Blocked a frame with origin "null" from accessing a frame with origin "null". 
Protocols, domains, and ports must match.
```

3. **已尝试的解决方案**（按时间顺序）：
   - 方案1：使用 `parent.location.href = parent.location.href` → 同样被 CORS 阻止
   - 方案2：使用 `parent.location.href='../../index.html'` → 同样被 CORS 阻止
   - 方案3：使用 `history.back()` → 无反应（因为模块页面不在历史记录栈中）
   - 方案4：使用 `window.top.location.href='../../index.html'` → 被阻止

4. **当前采用方案**：
   修改为直接使用 `location.href='../../index.html'`，绕过 parent 对象访问。

#### 涉及文件
- `modules/battery-test/index.html`
- `modules/busbar-check/index.html`
- `modules/elevator-escape/index.html`
- `modules/elevator-handrail-fault/index.html`
- `modules/generator/index.html`
- `modules/power-room-monthly/index.html`
- `modules/capacitor-check/module.js`（goBack 函数）

### 3.2 部分模块页面显示空白

#### 问题描述
卸载重装后，某些模块（如电池测试模块）首次进入时显示空白页面，无内容渲染。

#### 技术分析
1. **可能原因**：
   - JavaScript 执行出错但未捕获
   - CSS 文件加载失败
   - 模块初始化函数未执行

2. **排查情况**：
   - Logcat 中无任何 Error 或 CONSOLE 错误
   - 电脑端浏览器（localhost:8088）完全正常
   - 其他模块能正常显示

3. **可能影响**：
   - 电池测试模块页面空白可能与返回按钮错误存在关联
   - 错误的事件处理可能导致 JavaScript 执行中断

### 3.3 文件路径兼容性问题

#### 问题描述
模块中加载资源文件时使用相对路径 `../../assets/templates/xxx.xlsx`，在 web 环境正常，但打包为 APK 后可能路径解析错误。

#### 错误日志
```
Fetch API cannot load file:///android_asset/assets/templates/elevator_monthly_report.xlsx. 
URL scheme "file" is not supported.
```

#### 涉及文件
- `modules/elevator-handrail-fault/module.js`（已修复路径）
- `modules/capacitor-check/module.js`（已修复路径）
- `modules/busbar-check/module.js`（已修复路径）

#### 修复方案
将相对路径从 `../../assets/templates/` 改为 `../../../assets/templates/`（增加一层目录深度）。

---

## 四、权限配置

### 4.1 Android 权限声明
```xml
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.INTERNET" />
```

### 4.2 功能说明
- **CAMERA**：拍照功能
- **READ_EXTERNAL_STORAGE**：读取存储卡
- **WRITE_EXTERNAL_STORAGE**：写入存储卡
- **INTERNET**：网络访问（用于 OCR 云端识别）

---

## 五、WebView 配置

### 5.1 MainActivity.java 中的 WebView 设置
```java
webView.getSettings().setJavaScriptEnabled(true);
webView.getSettings().setAllowFileAccess(true);
webView.getSettings().setAllowContentAccess(true);
webView.getSettings().setDomStorageEnabled(true);
webView.getSettings().setDatabaseEnabled(true);
webView.getSettings().setCacheMode(WebSettings.LOAD_DEFAULT);
webView.loadUrl("file:///android_asset/index.html");
```

### 5.2 当前问题
- 文件 URL（file://）访问存在安全限制
- 需要确保 `android:usesCleartextTraffic="true"` 已配置

---

## 六、已知限制

### 6.1 OCR 功能
- Tesseract.js 需要下载语言模型（约 20MB）
- 当前模型文件：`eng.traineddata.gz`（约 10.9MB）
- 无 VPN 环境下无法下载更多语言包

### 6.2 电梯逃生门模块
- 当前为占位模块，功能简单
- 页面显示"正在开发中"

### 6.3 Excel 模板文件
- 部分模板文件名存在中文乱码问题
- 已使用英文别名（如 `generator_template.xlsx`）作为临时解决方案

---

## 七、开发环境

### 7.1 环境配置
- **Android Studio**：5.07 版本
- **JDK**：随 Android Studio 捆绑（JBR）
- **Android SDK**：`C:\Users\wq\AppData\Local\Android\Sdk`
- **平台版本**：Android 16（API 36）/ Android 14（API 34）
- **构建工具**：37.0.0

### 7.2 测试设备
- **手机型号**：vivo V2527A
- **系统版本**：Android 16（API 36）

---

## 八、建议的解决方案

### 8.1 关于返回按钮
1. **方案 A（推荐）**：修改 index.html 的 iframe 导航逻辑，监听 iframe 的 load 事件，在模块页面注入返回函数，而不是让模块页面主动访问 parent 对象。

2. **方案 B**：在 MainActivity 中重写 `onBackPressed()`，直接关闭 WebView 的 iframe 并返回主页，而不是依赖 JavaScript 处理。

3. **方案 C**：在模块页面完全不依赖 parent 访问，改为使用 `window.location.href` 配合 `postMessage` 与父页面通信。

### 8.2 关于页面空白
1. 检查电池测试模块的 JavaScript 是否存在语法错误或运行时错误
2. 添加 `try-catch` 捕获初始化过程中的异常
3. 在关键步骤添加 `console.log` 用于调试

### 8.3 关于文件路径
1. 考虑使用绝对路径 `/android_asset/`
2. 或在 WebView 设置中配置 `setAllowFileAccessFromFileURLs(true)`

---

## 九、联系方式

如需进一步帮助，请提供：
1. 完整的 Logcat 日志（过滤条件：com.example.propertymaintenance）
2. 出现问题的具体操作步骤
3. 期望的正常行为描述

---

**文档生成时间**：2026-04-25 15:15
**文档版本**：v1.0